# {{ ansible_managed }}

[user]
  name = {{ git_username }}
  email = {{ git_usermail }}

[color]
  ui = true

[core]
  editor = {{ git_coreeditor|default('vi') }}
  autocrlf = true

[push]
  default = {{ git_pushdefault|default('current')  }}

